"""
def sum (alist):
    if len(alist)==0:
        returb 0
    else:
        s+=alist[0]
        alist.remove(alist[0])
        return a+sum(alist)
"""
def Sum(alist):
    if len(alist) < 1:
        return 0
    else:
        for i in alist:
            aum += i
        return sum
        
alist = [2，3,4,5]
s=sum(alist)
print(s)
