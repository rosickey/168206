def qsort_(list)
    if len(list) = 1
        return list
    else
        mid = list[0]
        L = [i for i in list[1len(list)] if i = mid]
        H = [i for i in list[1len(list)] if i  mid]
        return qsort_(L) + [mid] + qsort_(H)
list = [4,1,3,22,8,2,14]
print (qsort_(list))
